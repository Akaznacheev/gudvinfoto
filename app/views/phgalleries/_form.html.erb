<%= form_for @phgallery, html: { multipart: true } do |f| %>
  <% if @phgallery.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@phgallery.errors.count, "error") %> prohibited this phgallery from being saved:</h2>

      <ul>
      <% @phgallery.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <%= f.label :images %>
  <%= f.file_field :images, multiple: true, accept: "image/jpeg, image/jpg, image/gif, image/png" %>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>