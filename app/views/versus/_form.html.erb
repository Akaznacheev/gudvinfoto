<%= form_for(@versu) do |f| %>
  <% if @versu.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@versu.errors.count, "error") %> prohibited this versu from being saved:</h2>

      <ul>
      <% @versu.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_area :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
