<div class="authform">
  <%= form_for(resource, :as => resource_name, :url => registration_path(resource_name), :html => { :method => :put, :role => 'form'}) do |f| %>
      <h3>НАСТРОЙКИ</h3>
      <% devise_error_messages! %>
      <div style="float: left; width: 48%;">
        <div class="form-group">
          <%= f.label 'ИМЯ ПОЛЬЗОВАТЕЛЯ' %>
          <%= f.text_field :name, :autofocus => true, class: 'form-control' %>
        </div>
      </div>
      <div style="float: right; width: 48%;">
        <div class="form-group">
          <%= f.label 'E-mail' %>
          <%= f.email_field :email, class: 'form-control' %>
          <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
              <div>Currently waiting confirmation for: <%= resource.unconfirmed_email %></div>
          <% end %>
        </div>
      </div>
      <div style="float: left; width: 48%;">
        <div class="form-group">
          <%= f.label 'НОВЫЙ ПАРОЛЬ' %>
          <%= f.password_field :password, :autocomplete => 'off', class: 'form-control' %>
        </div>
      </div>
      <div style="float: right; width: 48%;">
        <div class="form-group">
          <%= f.label 'ПОДТВЕРДИТЕ НОВЫЙ ПАРОЛЬ' %>
          <%= f.password_field :password_confirmation, class: 'form-control' %>
        </div>
      </div>
      <p style="text-align: center;">*ОСТАВЬТЕ ПОЛЯ ПУСТЫМИ ЕСЛИ НЕ СОБИРАЕТЕСЬ МЕНЯТЬ ПАРОЛЬ.</p>
      <div style="margin: auto; width: 50%;">
        <div class="form-group">
          <%= f.label 'ТЕКУЩИЙ ПАРОЛЬ', title: "ВЫ ДОЛЖНЫ ВВЕСТИ ТЕКУЩИЙ ПАРОЛЬ, ЧТОБЫ СОХРАНИТЬ ИЗМЕНЕНИЯ." %>
          <%= f.password_field :current_password, class: 'form-control', title: "ВЫ ДОЛЖНЫ ВВЕСТИ ТЕКУЩИЙ ПАРОЛЬ, ЧТОБЫ СОХРАНИТЬ ИЗМЕНЕНИЯ." %>
        </div>
      </div>
      <div style="width: 100%; text-align: center">
        <%= f.submit 'CОХРАНИТЬ', :class => 'button' %>
      </div>
  <% end %>
  <div class="userdelete">
    <fieldset>
      <h3>УДАЛЕНИЕ АККАУНТА</h3>
      <p>ЧТО-ТО НЕ ПОНРАВИЛОСЬ?<br> ЕСЛИ ВАМ БУДЕТ НУЖНА ФОТОКНИГА, МЫ БУДЕМ РАДЫ НОВОЙ ВСТРЕЧИ.</p>
      <%= link_to "УДАЛИТЬСЯ", registration_path(resource_name), :data => { :confirm => "Вы уверены?" }, :method => :delete, :class => 'button' %>
    </fieldset>
  </div>
</div>
<style>
  body {
    min-height: 100vh;
    font-family: 'PT Sans';
  }
  .authform {
    padding-top: 0;
    max-width: 30%;
    position: relative;
    top: 48%;
    transform: translate(0%, -50%);
  }
  .authform form {
    background:white;
    padding: 5%;
    border: 1px solid;
    border-color: #dddddd;
    border-radius: 5px;
  }
  .authform .userdelete {
    background:white;
  }
  .well, .authform form, .authform fieldset {
    margin-bottom: 1vw;
    padding: 5% 5% 0 5%;
  }
  .userdelete {
    margin-top: 5%;
    margin-bottom: 5%;
  }
  .userdelete fieldset {
    background: white;
    text-align: center;
  }
  p {
    font-size: 0.7vw;
  }
  h3 {
    margin: 3%;
    font-size: 1.2vw;
    text-align: center;
  }
  label {
    font-size: 0.75vw;
  }
  .form-group {
    margin-bottom: 5%;
  }
  .authform .button {
    padding: 0.4vw 0.8vw;
    font-size: 0.7vw;
    color: #fff;
    background-color: #71C8C6;
    border-color: #71C8C6;
    margin-bottom: 5%;
  }
  .authform .button:hover,.authform .button:focus,.authform .button:active {
    background-color: rgba(54, 150, 150, 0.7);
    border-color: rgba(54, 150, 150, 0.7);
  }
  .form-control {
    height: 1.5vw;
    padding: 0.4vw 0.8vw;
    font-size: 0.7vw;
  }
  .form-control:hover, .form-control:focus{
    border: 1px solid rgb(113, 200, 198);
  }
</style>